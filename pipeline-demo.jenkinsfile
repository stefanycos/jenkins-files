#!groovy

@Library('shared-library-demo')_

node {
    stage('Checkout') {
        echo 'Starting demo pipeline'
        checkout (
            branch : 'master',
            url : 'https://github.com/stefanycos/microservice-repo.git'
        )
    }
}
