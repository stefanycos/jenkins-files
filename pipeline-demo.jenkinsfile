#!groovy

@Library('shared-library-demo')_

node {
    stage('Demo') {
        echo 'Starting demo pipeline'
        checkout (
            branch : 'Testing',
            url : 'Test'
        )
    }
}
